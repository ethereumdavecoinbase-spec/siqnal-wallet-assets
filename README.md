# SIQNAL Wallet Assets

Official asset repository for SIQNAL Wallet.

## Structure

- `networks/` — network and native-asset icons
- `tokens/` — token icons organized by network and contract address

Token filenames use lowercase EVM contract addresses, including the `0x` prefix.
